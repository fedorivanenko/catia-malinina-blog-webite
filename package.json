{
  "name": "catia-malinina-blog",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "turbo": "^2.10.10"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.9.0"
  }
}